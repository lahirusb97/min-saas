PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_NAME=min-saas

JWT_SECRET=change-me

# Comma-separated list of allowed CORS origins. Defaults to
# https://app.eyevixa.com and local dev ports if unset.
CORS_ORIGINS=https://app.eyevixa.com,http://localhost:5173
