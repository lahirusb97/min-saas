export enum SourceType {
  INVENTORY = 'inventory',
  MANUAL = 'manual',
}

export enum LensSide {
  BOTH = 'Both',
  RIGHT = 'Right',
  LEFT = 'Left',
}

export enum OrderStatus {
  PENDING = 'Pending',
  DELIVERED = 'Delivered',
  CANCELLED = 'Cancelled',
}
